--- docker-entrypoint.sh	2015-06-16 07:40:18.000000000 +1000
+++ -	2015-06-18 15:47:47.000000000 +1000
@@ -50,6 +50,17 @@
 	fi
 fi

+echo "* Setting statsd"
+STATSD_ENABLED=${STATSD_ENABLED:-false}
+
+if ! grep ^statsd $CONFIG_FILE > /dev/null 2>&1; then
+	echo "statsd.enabled: $STATSD_ENABLED" >> $CONFIG_FILE
+	echo "statsd enabled: $STATSD_ENABLED" >> $CONFIG_FILE
+else
+	sed -i "s/^statsd\..*/statsd.enabled: $STATSD_ENABLED/g" $CONFIG_FILE
+	sed -i "s/^statsd .*/statsd enabled: $STATSD_ENABLED/g" $CONFIG_FILE
+fi
+
 if [ $# -eq 0 ]; then
 	if ! /usr/bin/sysdig-probe-loader; then
 		exit 1
